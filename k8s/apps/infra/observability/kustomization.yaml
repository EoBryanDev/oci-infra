apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ns.yaml
  # Onda 2+: kube-prometheus.yaml, loki.yaml, tempo.yaml, otel-collector.yaml,
  # grafana-ingressroute.yaml (1 Application Helm por componente, ordem de dependência).
