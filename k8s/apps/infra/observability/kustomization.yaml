apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ns.yaml
  - kube-prometheus.yaml
  # Onda 5+: loki.yaml, tempo.yaml, otel-collector.yaml,
  # grafana-ingressroute.yaml (1 Application Helm por componente, ordem de dependência).
